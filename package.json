{
  "name": "navop-js",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "workspaces": ["packages/*"],
  "engines": { "node": ">=20" },
  "scripts": {
    "build": "npm run build --workspace @navop/client && npm run build --workspace @navop/cli && npm run build --workspace @navop/mcp",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test test/*.test.mjs",
    "check": "npm run build && npm run typecheck && node --test test/*.test.mjs",
    "pack:dry-run": "npm pack --dry-run --workspace @navop/client && npm pack --dry-run --workspace @navop/cli && npm pack --dry-run --workspace @navop/mcp"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  },
  "license": "MIT"
}
